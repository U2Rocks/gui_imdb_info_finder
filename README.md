# gui_imdb_info_finder
A small GUI app that can find basic info using the movie database API.
Movie Database is used for the movie information instead of IMDB's API and RAPID API is used to manage the API
